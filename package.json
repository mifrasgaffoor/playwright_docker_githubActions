{
  "name": "playwright_docker_practice",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test --reporter=allure-playwright",
    "allure:generate": "npx allure-commandline generate ./allure-results --clean",
    "allure:serve": "npx allure-commandline open ./allure-report",
    "install": "npx playwright install --with-deps"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.51.1",
    "@types/node": "^22.13.10",
    "allure-playwright": "^3.2.0"
  },
  "dependencies": {
    "playwright_docker_practice": "file:"
  }
}
